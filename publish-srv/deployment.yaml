apiVersion: apps/v1
kind: Deployment
metadata:
  name: douyin-publish
  labels:
    app: publish-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: publish-server
  template:
    metadata:
      labels:
        app: publish-server
        version: v1
    spec:
      containers:
        - name: douyin-publish
          image: ccr.ccs.tencentyun.com/douyin-test/publish-srv:v1.0
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: 200Mi
              cpu: "0"
          ports:
            - containerPort: 8080
          env:
            - name: MYSQL_HOST
              value: "113.54.157.200"
            - name: MYSQL_PORT
              value: "3307"

---
apiVersion: v1
kind: Service
metadata:
  name: publish-balance-svc
  labels:
    name: publish-balance-svc
spec:
  #type: NodePort
  ports:
  - name: grpc-publish
    port: 8080
    targetPort: 8080
  clusterIP: None
  selector:
    app: publish-server
